import 'package:flutter/material.dart';

class PdfViewerScreen extends StatelessWidget {
  const PdfViewerScreen({required this.pdfId, super.key});

  final String pdfId;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
